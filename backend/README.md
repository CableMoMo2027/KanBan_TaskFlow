# Backend

NestJS backend for the Kanban project.

## Getting started

```bash
npm install
npm run start:dev
```

The server runs on `http://localhost:3001` by default.

## Endpoints

- `GET /`
- `GET /health`
